files used is cinemetography is in another branch(cinemotgraphy(+revs),
beside main.js ,and files in folder public, nothing else changed from the main branch

the file of objects( beaver, album, etc) are meant to be in the folder public.

:)))
